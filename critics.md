🔴 1. Critical: Resolve Prompt vs. Quant Engine Contradiction
Issue: Your System Prompt.txt (Rule 16 & 17) instructs the AI to "apply these sizing modifiers to every BUY/TOP_UP before finalising qty" (e.g., reduce qty by 25% if VaR1d < -3.5%). However, CLAUDE.md explicitly states under the AI Pipeline: "Quant engine: Deterministic sizing... Never does arithmetic." LLMs are notoriously bad at reliable arithmetic, and asking it to calculate adjusted quantities violates your own deterministic design.
Improvement:
Remove the arithmetic instruction from the System Prompt.
Instead, have the Prompt output a sizeModifier flag or riskAdjustmentReason (e.g., "HIGH-VAR: reduce size").
Let quant-engine.js handle the actual math deterministically based on the PORTFOLIO RISK METRICS provided in the user message, ensuring the final qty is always mathematically sound before validation.
🟠 2. Address "Open" Roadmap Items (from IMPROVEMENTS.md)
Your docs list three open items. Here is how to tackle them:
Local LLM SELL/TRIM Support (Discrepancy): IMPROVEMENTS.md lists this as "Open" but also notes it was "SHIPPED Sprint 48". Action: Audit routes/debate.py and js/prompts.js to ensure _LOCAL_ANALYSIS_SYSTEM and _SCHEMA_QUICK_ANALYSIS fully support the 5-driver taxonomy for exits without falling back to Claude. If it’s truly shipped, update the "Open" section to prevent future confusion.
Mergers/Takeovers CGT Gap: yfinance does not tag stock-to-cash corporate actions, breaking cost-basis tracking. Action: Add a "Manual Corporate Action" modal in the Portfolio/CGT page. Allow the user to input {ticker, eventDate, cashPerShare, newTicker?}. The backend should then generate a synthetic tradeJournal entry to close the parcel and adjust the cash balance, ensuring EOFY tax packs remain accurate.
ES-Modules Migration: Deferred due to risk. Action: Instead of a big-bang rewrite, introduce a bundler (like Vite or Rollup) in a new branch. Let it handle the dependency graph of your existing vanilla JS files without forcing you to rewrite import/export syntax immediately. This gives you dead-code elimination and safer loading order without the 27-file refactor risk.
🟡 3. Learning Loop & Calibration Enhancements
Dynamic Virtual Outcome Cutoff: Currently, _resolve_virtual_outcomes() uses a hard 30-day cutoff (learning_loop.md). A 6-month swing trade flagged as virtual_open at day 31 is a false negative. Improvement: Make the cutoff dynamic: max(30, expectedTimeToTarget * 1.5). This gives longer-term theses adequate time to resolve before being marked as unresolved.
Phase 8 Transparency: The Mann-Whitney U gate (z > 1.28) is a great statistical guard. Improvement: Expose the exact z-score and p-value in the Learning Loop UI (e.g., "Phase 8 Inactive: z=0.85. Win skills not yet statistically higher than loss skills"). This builds trust and explains why skill-weighting isn't amplifying yet.
Sell Tag Validation Edge Case: learning_loop.md notes that better_opportunity requires an alternative_ticker. Improvement: Add a frontend validation in analysis.js that blocks the recommendation from rendering if primary_driver === 'better_opportunity' but alternativeTicker is null/undefined, preventing bad data from entering the learning loop.
🟢 4. Data & Reliability Tweaks
SPI200 Futures Data Gap: CLAUDE.md notes that YAP=F (SPI200) is unsupported by the Stooq fallback. Since SPI200 is explicitly used as a "same-session lead indicator" vote in regime-engine.js, a missing value degrades regime classification during yfinance outages. Improvement: Add a lightweight, dedicated fallback for YAP=F (e.g., a simple scrape of a free financial site like Investing.com or MarketWatch, cached for 5 minutes) to ensure the regime engine never loses this critical macro signal.
_detectExitReason Shadowing: CLAUDE.md mentions the function was hoisted to utils.js in Sprint 43, but warns: "If either page file accidentally re-introduces a local definition it will shadow the canonical copy". Improvement: Add an ESLint rule (no-redeclare) or a simple runtime check in recommendations.js and performance.js to throw a console error if _detectExitReason is defined locally, guaranteeing the canonical version is always used.
🔵 5. Prompt Engineering Refinements
Rule 15 Override Clarity: The System Prompt mentions comparing "estimated tax saving vs bear-case expected further loss (bear_prob × qty × entry × bear_ret)". LLMs struggle with multi-step algebraic formulas in text. Improvement: Provide a concrete, filled-out example in the prompt.
Example: "If tax saving = $400, and bear-case expected loss = 0.25 (bear_prob) × 100 (qty) × $50 (entry) × 0.20 (bear_ret) = $250, then $400 > $250, so WAIT for CGT discount."
Anti-Churn Rule 1 Edge Case: The prompt blocks SELL/TRIM within 7 days of a BUY unless a "CATASTROPHE" occurs. Improvement: Explicitly add "Brokerage cost exceeds potential profit" to the list of valid reasons to override the 7-day block. If the AI realizes a trade was a complete mistake and the round-trip $20 fee is the only thing stopping an exit, it should be allowed to flag it as a "risk_management" override rather than being forced to hold a toxic asset.
